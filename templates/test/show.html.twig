{% extends 'base.html.twig' %}

{% block title %}Test{% endblock %}

{% block body %}
    <h1>Test</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ test.id }}</td>
            </tr>
            <tr>
                <th>NomTest</th>
                <td>{{ test.nomTest }}</td>
            </tr>
            <tr>
                <th>DescriptionTest</th>
                <td>{{ test.descriptionTest }}</td>
            </tr>
            <tr>
                <th>PrixTest</th>
                <td>{{ test.prixTest }}</td>
            </tr>
            <tr>
                <th>StockTest</th>
                <td>{{ test.stockTest ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_test_index') }}">back to list</a>

    <a href="{{ path('app_test_edit', {'id': test.id}) }}">edit</a>

    {{ include('test/_delete_form.html.twig') }}
{% endblock %}
